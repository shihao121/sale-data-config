#redis
spring:
  redis:
    database: 0
    host: localhost
    port: 6379
    password:
    max-active: 5
    max-wait: 1000
    max-idle: 8
    min-idle: 3
    timeout: 60000

  application:
    name: sale-data-spareclient
server:
  port: 8083

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8281/eureka/
